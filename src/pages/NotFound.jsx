import { NotFoundSection } from '@features/error';
import Layout from '@layout/Layout';

const NotFound = () => {
  return (
    <Layout>
      <NotFoundSection />
    </Layout>
  );
};

export default NotFound;
