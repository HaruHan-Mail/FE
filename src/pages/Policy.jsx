import Layout from '@layout/Layout';
import { PolicyComponent } from '@features/policy';

const Policy = () => {
  return (
    <Layout>
      <PolicyComponent />
    </Layout>
  );
};

export default Policy;
